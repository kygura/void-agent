# Changelog

## [Unreleased]

### Added

- Added the `@void/jev` package: a dependency-free, response-validating client for TypeSafe's System One API (`POST /v1/systemone`) with typed `noul`, `choice`, and `score` questions.
- Added `createToolCallJudge` and the pure `decideToolCall` policy for screening agent tool calls before they run.
